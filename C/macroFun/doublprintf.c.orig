#include <stdio.h>
#define DBL_PRINTF(human, ...) do {printf human;\
									fprintf(stderr,__VA_ARGS__);} while(0)
aaayyy lllmmmaaaooo
int main()
{
	int x = -1;
	if (x < 0) DBL_PRINT(("x is negative %g\n",x),"NEGVAL: x=%g\n",x);
}
